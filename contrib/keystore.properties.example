# Copy this to a private location (outside the repo) and point
# RECAPTCHA_HELPER_KEYSTORE_PROPERTIES at it when building a signed release:
#
#   RECAPTCHA_HELPER_KEYSTORE_PROPERTIES=/path/to/keystore.properties \
#     ./gradlew :app:assembleRelease
#
# app/build.gradle.kts reads it directly at build time; without it the release
# APK is left unsigned. Never commit your real JKS or this file with real values.

# storeFile may use `~/` for your home directory.
# This must be the same keystore that signs Open Grind releases, so storeFile
# and keyAlias match the ones you created there.
storeFile=~/.config/open-grind/release.jks
keyAlias=open-grind
password=change-me
